Procedure:9

GOAL RECONSTRUCTION: Camera Pose Reconstruction

STEP 1: Read in Camera data
- Set the image path to the provided directory of images to the CameraDataManager to read, resize, and pre-process images for reconstruction
  - image_path = C:\Users\Anthony\Documents\Projects\datasets\sfm_dataset\Experiments\co3dv2\orange\374_42196_84367\images
- Set max_images=20 (Never above 40) to keep a short, incremental sequence around the object that satisfies the Essential→PnP assumption.
- Set the calibration path to the provided calibration file. Since it is provided, we have a calibrated monocular camera and activate the parameter
  - calibration_path = C:\Users\Anthony\Documents\Projects\datasets\sfm_dataset\Experiments\co3dv2\orange\calibration_new_374_42196_84367.npz
- If available in the manager, downscale long side to ~1024 px to remain GPU/CPU friendly while preserving feature quality.
- Finally, call get_camera_data() to have access to the camera data of images and calibration
  - Reasoning: We are targeting accurate, drift-minimized extrinsics with known intrinsics; no learning-based calibration is required and we keep the sequence length modest for incremental motion.

STEP 2: Detect Features
- Select the SuperPoint detector via FeatureDetectionSP because the target object (an orange) is moderately textured with diffuse lighting and few crisp corners; SuperPoint is robust on smooth, repetitive, or low-contrast texture where SIFT may under-detect.
  - Set max_keypoints = 6000
    - Reasoning: We want dense, robust keypoints on the weakly textured orange and tabletop to ensure reliable correspondences for PnP and later tracking.

STEP 3: Detect Feature Matches (pairwise, sequential frames)
- Use FeatureMatchSuperGluePair with detector='superpoint' to obtain accurate pairwise correspondences for pose initialization; SuperGlue is preferred over LightGlue here because PnP is sensitive to outliers and we prioritize match accuracy over speed for this short sequence.
  - Set setting = "indoor"
    - Reasoning: Captured indoors; use indoor weights for best robustness.
  - Set RANSAC = True
  - Set RANSAC_threshold = 0.03 (normalized coordinates)
    - Reasoning: Tight epipolar inlier threshold to aggressively remove outliers while preserving sufficient inliers for PnP on this calibrated setup.
  - Keep sinkhorn_iterations = 20 and match_threshold = 0.2 (defaults)
    - Reasoning: Defaults provide a good accuracy/speed balance for short sequences.

STEP 4: Estimate the camera pose using detected matching feature pairs
- Use CamPoseEstimatorEssentialToPnP to bootstrap from the essential matrix on the first pair and continue incrementally with PnP; the motion between consecutive views is small and consistent around the object, matching the module’s assumptions. Activate local bundle adjustment to reduce drift.
  - Initialize BundleAdjustmentOptimizerLocal
    - refine_focal_length = False, refine_principal_point = False, refine_extra_params = False
      - Reasoning: Intrinsics are known and should remain fixed for stable extrinsics in a calibrated pipeline.
    - max_num_iterations = 30
      - Reasoning: Sufficient to significantly reduce local reprojection error without heavy runtime.
    - window_size = 12
      - Reasoning: Slightly larger window for stronger local pose regularization and drift suppression over the short object loop.
    - use_gpu = True, robust_loss = True
      - Reasoning: Robust Cauchy loss improves resilience to residual outliers.
  - CamPoseEstimatorEssentialToPnP parameters:
    - reprojection_error = 3.0 (pixels)
      - Reasoning: Standard threshold; we already enforce strict geometric filtering in matching.
    - iteration_count = 300
      - Reasoning: Increased LM iterations improve convergence with SuperPoint-derived correspondences.
    - confidence = 0.995
      - Reasoning: Higher success probability for RANSAC model selection on a calibrated sequence.
    - optimizer = BundleAdjustmentOptimizerLocal object from above
      - Reasoning: To continually refine poses and limit drift during incremental PnP.

STEP 5: Track Features across multiple images to create stable multi-view tracks (supporting landmark consistency and later global BA)
- Use FeatureMatchLightGlueTracking with detector='superpoint' to efficiently build multi-view tracks across the sequence.
  - Set RANSAC_threshold = 0.05 (normalized coordinates)
    - Reasoning: Slightly looser than pairwise matching to favor longer tracks over time while still removing gross outliers.
  - Keep n_layers = 9, flash = True, and default confidences
    - Reasoning: Balanced speed/accuracy on 12 GB GPU while maintaining good track lengths.

STEP 6: Build a lightweight sparse structure to stabilize the trajectory (optional outputs; used to refine poses)
- Use Sparse3DReconstructionMono for classical multi-view triangulation with the tracked features and the poses from Step 4.
  - Set view = "multi"
    - Reasoning: We will triangulate using multi-view tracks from Step 5.
  - Set min_observe = 3
    - Reasoning: Require at least three observations to accept a 3D point while keeping enough points on the weakly textured orange.
  - Set min_angle = 1.5
    - Reasoning: Modest baseline constraint to avoid unstable rays on near-planar/tabletop regions yet keep enough points for optimization.
  - Set reproj_error = 3.0 (pixels)
    - Reasoning: Aligns with the pose estimation threshold to maintain a consistent inlier definition.

STEP 7: Apply Global Bundle Adjustment to minimize drift and deliver the best trajectory
- Use BundleAdjustmentOptimizerGlobal on the sparse scene from Step 6 to jointly refine all camera extrinsics and 3D points.
  - refine_focal_length = False, refine_principal_point = False, refine_extra_params = False
    - Reasoning: Calibrated camera; keep intrinsics fixed for a stable, scale-consistent trajectory.
  - max_num_iterations = 150
    - Reasoning: Enough iterations to converge globally given moderate keypoint noise and mixed texture.
  - use_gpu = True, robust_loss = True
    - Reasoning: Robust loss mitigates any lingering outlier projections and improves final trajectory smoothness.

STEP 8: Output
- Deliver the optimized camera extrinsics (trajectory) from the final optimized scene (cam_poses). The sparse landmarks are auxiliary for stability; the required deliverable is the per-frame camera pose sequence in a consistent object/world frame.
  - Reasoning: The task is pose-only reconstruction on a calibrated monocular sequence. Metric scale may remain unobservable; relative scale is consistent across frames due to Essential→PnP initialization and bundle adjustment.

Notes and safeguards
- We explicitly avoid CamPoseEstimatorVGGTModel due to the 12 GB GPU constraint and because intrinsics are known; the classical feature-based pipeline is preferred.
- Ensure the image order reflects incremental motion around the object to respect CamPoseEstimatorEssentialToPnP assumptions. If the sequence contains large jumps, split into overlapping subsets and run Steps 3–7 per subset before merging trajectories with shared frames.